; Dev-only OPcache overrides — mounted in dev compose only.
; Disables the validate_timestamps=0 prod setting so file changes
; are picked up immediately without restarting the container.
opcache.validate_timestamps = 1
opcache.revalidate_freq = 0
